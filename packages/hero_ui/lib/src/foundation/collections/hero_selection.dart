/// How many items of a collection can be selected (React Aria's
/// `selectionMode`).
enum HeroSelectionMode {
  /// Items cannot be selected.
  none,

  /// At most one item is selected at a time.
  single,

  /// Any number of items can be selected.
  multiple,
}
